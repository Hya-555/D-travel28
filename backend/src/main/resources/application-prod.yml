# =====================================================
# 生产环境配置 - MySQL
# =====================================================
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/travel_system?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=UTF-8
    username: root
    password: ${DB_PASSWORD:rsz123456}
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: never

logging:
  level:
    com.travel: info
